$("#tipo_personal").on("change", function (event) {
    let tipo = $("#tipo_personal").val();
    console.log(tipo);
    // if (tipo == 1) {
    //     $('#conductor').removeAttr('hidden', false);
    // }
});
